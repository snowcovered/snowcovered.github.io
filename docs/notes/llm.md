# 版本化大模型

我们目前可以认为就是以固定收费+线性可变收费贩卖一个模型。

contractible：某个变量或行为能否被写进合同并且可以被验证和执行。

我们需要考虑的用户具有**异质性**：

1. 用户对输入、输出、微调的偏好。
2. 对任务量有规模的偏好w。

本文考虑两种定价：

1. 允许任务在不同任务见分配，只要求Z,Y,Z总量菜单
2. 不允许任务不同任务分配，菜单具体到分配里。









